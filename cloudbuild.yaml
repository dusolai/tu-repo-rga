steps:
  # Paso 1: Construir la imagen Docker
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/backend-cerebro:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/backend-cerebro:latest'
      - '-f'
      - './backend/Dockerfile'
      - './backend'

  # Paso 2: Subir la imagen al registro
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/backend-cerebro:$COMMIT_SHA'

  # Paso 3: Desplegar en Cloud Run (CORREGIDO: UPDATE EN LUGAR DE SET)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'backend-cerebro'
      - '--image=gcr.io/$PROJECT_ID/backend-cerebro:$COMMIT_SHA'
      - '--region=europe-southwest1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--update-env-vars=GEMINI_API_KEY=${_GEMINI_API_KEY}'  # <--- AQUÍ ESTABA EL PROBLEMA

# Configuración de imágenes a mantener
images:
  - 'gcr.io/$PROJECT_ID/backend-cerebro:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/backend-cerebro:latest'

# Sustituciones
substitutions:
  _GEMINI_API_KEY: 'PLACEHOLDER'

# Opciones
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
